[package]
name = "rusty-golem"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
chrono = "0.4"
reqwest = { version = "0.11", features = ["blocking", "json"] }
